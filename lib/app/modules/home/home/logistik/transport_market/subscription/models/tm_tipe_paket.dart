enum TMTipePaketSubscription {
  FIRST,
  ACTIVE,
  WILL_EXPIRED,
  EXPIRED,
}

enum TMTipeLayananSubscription {
  BF,
  SU,
}

enum TMTipeDetailSubscription {
  PEMBAYARAN,
  LANGGANAN,
  PESANAN,
}

enum TMTipeCurrentSubscription {
  NOW,
  NEXT,
}
