enum TipePaketSubscription {
  FIRST,
  ACTIVE,
  WILL_EXPIRED,
  EXPIRED,
}

enum TipeLayananSubscription {
  BF,
  SU,
}

enum TipeDetailSubscription {
  PEMBAYARAN,
  LANGGANAN,
  PESANAN,
}

enum TipeCurrentSubscription {
  NOW,
  NEXT,
}
